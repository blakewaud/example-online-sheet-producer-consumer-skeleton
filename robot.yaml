tasks:
  Produce:
    robotTaskName: Create work items from sheet

  Consume:
    robotTaskName: Load and Process All Orders

artifactsDir: output
condaConfigFile: conda.yaml
ignoreFiles: [.gitignore]

PATH: [.]
PYTHONPATH: [.]
